upstream frontend_upstream {
    server 127.0.0.1:3000;
    keepalive 32;
}

upstream gateway_upstream {
    server 127.0.0.1:8000;
    keepalive 32;
}

# HTTP-only server for EC2 public IP (no TLS yet)
# Replace 15.188.62.236 with your domain when HTTPS is configured.
server {
    listen 80;
    server_name 15.188.62.236;

    client_max_body_size 10M;

    location /api/ {
        proxy_pass http://gateway_upstream;
        include /etc/nginx/snippets/proxy-params.conf;
    }

    location / {
        proxy_pass http://frontend_upstream;
        include /etc/nginx/snippets/proxy-params.conf;
    }
}

# Redirect HTTP to HTTPS (enable when using a domain + certbot)
server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name your-domain.com;

    ssl_certificate     /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    client_max_body_size 10M;

    # Rate limit auth endpoints
    location /api/v1/auth/login {
        limit_req zone=auth_limit burst=5 nodelay;
        proxy_pass http://gateway_upstream;
        include /etc/nginx/snippets/proxy-params.conf;
    }

    location /api/ {
        proxy_pass http://gateway_upstream;
        include /etc/nginx/snippets/proxy-params.conf;
    }

    location / {
        proxy_pass http://frontend_upstream;
        include /etc/nginx/snippets/proxy-params.conf;
    }
}

# Add to /etc/nginx/nginx.conf http block:
# limit_req_zone $binary_remote_addr zone=auth_limit:10m rate=10r/m;
