apiVersion: apps/v1
kind: Deployment
metadata:
  name: showcase-service
  namespace: bootcamp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: showcase-service
  template:
    metadata:
      labels:
        app: showcase-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        fsGroup: 999
      containers:
        - name: showcase-service
          image: bootcamp-showcase-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: DATABASE_URL
              valueFrom:
                configMapKeyRef:
                  name: bootcamp-config
                  key: SHOWCASE_DATABASE_URL
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: bootcamp-config
                  key: LOG_LEVEL
            - name: ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: bootcamp-config
                  key: ENVIRONMENT
          livenessProbe:
            httpGet:
              path: /live
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: showcase-service
  namespace: bootcamp
spec:
  selector:
    app: showcase-service
  ports:
    - port: 8000
      targetPort: 8000
